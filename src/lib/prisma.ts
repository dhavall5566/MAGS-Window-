/**
 * Prisma is disabled. The app uses in-memory mock data from @/lib/mock-data.
 * Re-enable when connecting to PostgreSQL.
 */
// import { PrismaClient } from "@prisma/client";
// export const prisma = new PrismaClient();
